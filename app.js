const card = document.getElementsByClassName("card shadow");
const ekle = document.getElementsByClassName("ekle");
const ac = document.getElementsByClassName("btn btn-light position-relative");
class alisveris {
    constructor(image, title, price) {
        this.image = image;
        this.title = title;
        this.price = price;

    }
}
for (leti = 0; i < card.length; i++) {
    console.log(card[i]);
}